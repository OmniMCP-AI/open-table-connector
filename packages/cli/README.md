# Open Connectors CLI

`otc` is the short command for the Open Table Connector CLI.

This package currently scaffolds the shared CLI model layer used by the later
command, format, registry, and pipeline tasks.

The supported entry-point names are:

- `otc`
- `open-table-connector`
- `open-connectors`

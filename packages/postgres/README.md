# open-table-connector-postgres

PostgreSQL reader, temporal lowering, and managed snapshot storage.

Install with `pip install open-table-connector-postgres`; import
`open_table_connector.postgres`.

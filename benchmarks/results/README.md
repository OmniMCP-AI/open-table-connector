# Benchmark result artifacts

JSON artifacts record the suite, phase, workload, runtime/environment, raw
nanosecond samples, and Python peak allocations. Before/after comparisons must
use matching environment and workload metadata; a result is evidence, not a
performance threshold or a statistical-significance claim.

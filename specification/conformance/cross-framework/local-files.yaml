connector:
  id: local_files
  version: 0.1.0
contract_version: "1.0"
fixtures:
  - id: decimal-null-order
    relative_path: decimal-null-order.csv
    expected_columns: [id, amount, label]
    expected_rows: 2
    expected_mode: sheet
  - id: multi-sheet
    generated: true
    expected_sheets: [orders, returns]
    expected_mode: sheet
comparisons:
  - arrow_schema
  - polars_schema
  - decimal_precision_scale
  - nulls
  - row_order
  - coordinate_convention
  - source_revision
  - content_fingerprint
  - receipt_facts
framework_artifacts:
  finclaw: excluded
  open_time_series: excluded

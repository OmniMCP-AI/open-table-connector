# open-table-connector-conformance

Conformance fixtures and assertions for Connector implementations.

Install with `pip install open-table-connector-conformance`; import
`open_table_connector.conformance`.

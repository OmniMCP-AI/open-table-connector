# open-table-connector-contract

Framework-neutral Connector contracts, identities, receipts, and URI types.

Install with `pip install open-table-connector-contract`; import
`open_table_connector.contract`.
